<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.rahmabouraoui.foxsoundi">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".GenresActivity"
                android:label="Page de Genres"
                android:parentActivityName=".MainActivity">
        </activity>
        <activity
                android:name=".Top50Activity"
                android:label="Page de Top 50"
                android:parentActivityName=".MainActivity"/>
        <activity
                android:name=".NewActivity"
                android:label="Page de New"
                android:parentActivityName=".MainActivity"/>
        <activity
                android:name=".OptionsActivity"
                android:label="Plus d&apos;options"
                android:parentActivityName=".MainActivity"/>
        <activity
                android:name=".AccountActivity"
                android:label="@string/title_activity_account"
                android:parentActivityName=".MainActivity">
        </activity>
        <activity
                android:name=".SearchActivity"
                android:label="Page de search"
                android:parentActivityName=".OptionsActivity"/>
        <activity
                android:name=".YourMusicActivity"
                android:label="Page de your music"
                android:parentActivityName=".OptionsActivity"/>
    </application>

</manifest>
